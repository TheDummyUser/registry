import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const UserAttendence = () => {
  return (
    <View style={styles.container}>
      <Text>user attendence</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default UserAttendence;
