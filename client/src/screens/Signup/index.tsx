import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import Container from '~/components/container';

const Signup = () => {
  return (
    <Container>
      <Text>signup bich</Text>
    </Container>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default Signup;
